# Optional for local dev. Dev defaults to SQLite at prisma/dev.db (see lib/env-bootstrap.ts).
# Required for deployment: use a persistent managed DB URL (for example PostgreSQL).

# DATABASE_URL="file:./dev.db"
# DATABASE_URL="postgresql://user:pass@host:5432/dbname"

AUTH_SECRET=""
AUTH_URL="http://localhost:3000"
